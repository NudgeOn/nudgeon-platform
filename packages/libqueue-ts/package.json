{
  "name": "@onda/libqueue",
  "version": "0.1.0",
  "private": true,
  "description": "Redis Streams 큐 래퍼 (TS) — 큐 접근의 유일한 경로 (ADR-1)",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo 'no lint yet'"
  },
  "dependencies": {
    "@onda/queue-schemas": "workspace:*",
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}

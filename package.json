{
  "name": "nudgeon",
  "private": true,
  "version": "0.1.0",
  "description": "NudgeOn — open-source customer engagement platform for events, audiences, journeys, and message delivery",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
